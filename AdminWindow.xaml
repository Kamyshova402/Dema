<Window x:Class="Dema.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dema"
        mc:Ignorable="d"
        Title="AdminWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Управление пользователями" 
                   FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock x:Name="lblStats" Grid.Row="1" Text="Всего пользователей: 0" 
                   FontSize="13" Foreground="Gray" Margin="0,0,0,10" HorizontalAlignment="Center"/>

            <ListBox x:Name="lstUsers" Grid.Row="2" FontSize="14" Margin="0,10"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button x:Name="btnUnblock" Content="🔓 Разблокировать" Width="130" Margin="5" Padding="8" FontSize="13"/>
                <Button x:Name="btnDelete" Content="🗑 Удалить" Width="100" Margin="5" Padding="8" FontSize="13"/>
                <Button x:Name="btnAdd" Content="➕ Добавить" Width="100" Margin="5" Padding="8" FontSize="13"/>
                <Button x:Name="btnRefresh" Content="🔄 Обновить" Width="100" Margin="5" Padding="8" FontSize="13"/>
                <Button x:Name="btnExit" Grid.Row="3" Content="🚪 Выйти" Height="40" Width="120" FontSize="14" HorizontalAlignment="Center" Background="#FF5722" Foreground="White" Click="btnExit_Click"/>
            </StackPanel>
        </Grid>
        
    </Grid>
</Window>
